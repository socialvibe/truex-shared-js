{
  "name": "@socialvibe/shared",
  "version": "1.11.7",
  "description": "Common ES6 JavaScript library shared across TrueX repos",
  "private": true,
  "type": "module",
  "files": [
    "src"
  ],
  "main": "./src/",
  "scripts": {
    "test:only": "node --test --test-only",
    "test:single": "node --test",
    "test": "node --test 'src/**/__tests__/*-test*.js'",
    "test:ci": "node --test --test-reporter=spec --test-reporter-destination=stdout --test-reporter=junit --test-reporter-destination=test-results/junit.xml 'src/**/__tests__/*-test*.js'",
    "watch": "node --test --watch 'src/**/__tests__/*-test*.js'",
    "coverage": "node --test --experimental-test-coverage 'src/**/__tests__/*-test*.js'"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "global-jsdom": "^27.0.0",
    "jsdom": "^27.4.0"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}
